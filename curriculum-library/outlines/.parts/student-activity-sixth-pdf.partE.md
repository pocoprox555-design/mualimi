# جزء E — كتاب النشاط للصف السادس الإعدادي (الصفحات الفيزيائية 109–135 / المطبوعة 112–138)

> **القاعدة الرقمية:** الحقل `PRINTED_PAGE` فارغ (`none`) في كل هذه الصفحات، لذا **المطبوعة = الفيزيائية + 3** (109→112 … 135→138).
> **حالة الاستخراج:** كل الصفحات الـ 27 في هذا النطاق تحمل `(NO TEXT — IMAGE PAGE)` في الملف `student-activity-sixth-pdf.pages.txt`، وفي ملف JSON المرافق: `fullText` بطول 0 حرف، `searchable: false`، `needsOcr: true`، `ocr.status: needed`، `pageType: image`، وعنوان الصفحة `صفحة مصورة` وملخصها `تحتاج هذه الصفحة إلى القراءة البصرية من صورة PDF.`
> **لذا:** لا يوجد أي حرف تمرين قابل للاستخراج في هذا الجزء، ويجب عدم تخمين أي تمرين. عدد الصفحات المصورة = 27، عدد الصفحات النصية = 0.
> **السياق قبل/بعد النطاق:** تنتهي الصفحة الفيزيائية 106 (المطبوعة 109) بتمرين **WRITING** (وصف إجازة 100–120 كلمة)؛ وتمتد كتلة الصفحات المصورة حتى الصفحة الفيزيائية 164، ثم يعود النص في الصفحة الفيزيائية 165 (المطبوعة 168).

### الصفحة الفيزيائية 109 — المطبوعة 112
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 110 — المطبوعة 113
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 111 — المطبوعة 114
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 112 — المطبوعة 115
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 113 — المطبوعة 116
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 114 — المطبوعة 117
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 115 — المطبوعة 118
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 116 — المطبوعة 119
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 117 — المطبوعة 120
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 118 — المطبوعة 121
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 119 — المطبوعة 122
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 120 — المطبوعة 123
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 121 — المطبوعة 124
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 122 — المطبوعة 125
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 123 — المطبوعة 126
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 124 — المطبوعة 127
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 125 — المطبوعة 128
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 126 — المطبوعة 129
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 127 — المطبوعة 130
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 128 — المطبوعة 131
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 129 — المطبوعة 132
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 130 — المطبوعة 133
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 131 — المطبوعة 134
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 132 — المطبوعة 135
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 133 — المطبوعة 136
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 134 — المطبوعة 137
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

### الصفحة الفيزيائية 135 — المطبوعة 138
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج (لا عنوان مستخرج؛ حقل العنوان في المصدر `صفحة مصورة`)
- **التمارين**: لا يوجد — لا نص مستخرج؛ أي تمرين هنا غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة قبل قراءتها بصريًا من الـ PDF (`needsOcr: true`).

<!-- PART E END -->
