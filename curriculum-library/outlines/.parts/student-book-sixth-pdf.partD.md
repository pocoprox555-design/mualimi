# جزء D — كتاب الطالب للصف السادس الإعدادي (الصفحات الفيزيائية 61 – 80 / المطبوعة 65 – 84)
# المصدر: student-book-sixth-pdf.pages.txt (119 صفحة؛ قاعدة المستخرج: المطبوعة = الفيزيائية + 4 عند امتلاء الحقل PRINTED_PAGE بـ none)
# ملاحظة على النطاق: الصفحات 61–80 كلها معلّمة في المصدر `searchable: false | needsOcr: true` ونصها حرفياً `(NO TEXT — IMAGE PAGE)` أي صور بلا نص مستخرج؛ ولا يظهر في هذا النطاق أي عنوان "Unit 4" أو "Unit 5" لأن الوحدتين تنتهيان قبله: آخر صفحة بنص في الوحدتين هي الصفحة الفيزيائية 52 وفيها حرفياً `Unit 5 57 ... Round up | Revision | AB 104–105`.

### الصفحة الفيزيائية 61 — المطبوعة 65
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 62 — المطبوعة 66
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 63 — المطبوعة 67
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 64 — المطبوعة 68
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 65 — المطبوعة 69
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 66 — المطبوعة 70
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 67 — المطبوعة 71
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 68 — المطبوعة 72
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 69 — المطبوعة 73
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 70 — المطبوعة 74
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 71 — المطبوعة 75
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 72 — المطبوعة 76
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 73 — المطبوعة 77
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 74 — المطبوعة 78
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 75 — المطبوعة 79
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 76 — المطبوعة 80
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 77 — المطبوعة 81
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 78 — المطبوعة 82
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 79 — المطبوعة 83
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

### الصفحة الفيزيائية 80 — المطبوعة 84
- **النوع**: صفحة مصورة
- **العنوان الرسمي**: غير واضح من النص المستخرج
- **المحتوى**: لا نص مستخرج (تحتاج قراءة بصرية).
- **التمارين**: غير واضح من النص المستخرج
- **ملاحظة للاستخدام**: لا يمكن الإجابة عن أي سؤال من هذه الصفحة من النص المستخرج؛ الصفحة معلّمة في المصدر `searchable: false | needsOcr: true` ونصها `(NO TEXT — IMAGE PAGE)`، فلا تُستخرج منها تمارين ولا بنود إلا بعد قراءة بصرية أو OCR.

<!-- PART D END -->
