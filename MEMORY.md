# ذاكرة مشروع معلمي

آخر تحديث: 2026-09-25

هذا الملف هو سجل الذاكرة العملي للمحادثة وإعادة بناء المشروع. يجب قراءته قبل أي تعديل لاحق حتى لا تعود القرارات القديمة أو تتكرر الأخطاء التي عولجت.

## هوية المشروع

- الاسم: معلمي.
- الغرض: منصة تعلم عربية للسادس الإعدادي العراقي.
- النشر: Render.
- رابط التطبيق: https://mualimi.onrender.com
- المستودع: https://github.com/pocoprox555-design/mualimi
- الفرع: `main`.
- التشغيل: Node.js 22، خادم HTTP أصلي بصفر اعتماديات تشغيلية.
- Docker: `node:22-alpine`.
- آخر commit مرفوع: `ce58107 إعادة بناء معلمي وتبسيط إعداد المفتاح`.

## طلب المستخدم الأصلي

المستخدم أوضح أن التطبيق السابق خربان تماما، وطلب فهم فكرته ثم إعادة بنائه بالكامل من الصفر بطريقة احترافية وقوية، وليس ترقيع التطبيق القديم.

المطلوب كان:

- تطبيق سريع ومستقر ولا يفشل بسهولة.
- كود عملي ومختصر بلا أسماء وطبقات كبيرة بلا فائدة.
- واجهات مخصصة للجوال أولا، مرتبة وأنيقة ومريحة.
- رسائل وإجابات تظهر بشكل فاخر وواضح.
- المساعد يتصرف كمعلم كامل وذكي.
- يستطيع فهم السؤال والعثور على الجواب من الكتب تلقائيا من دون إجبار المستخدم على ذكر رقم الصفحة.
- تحسين الفهارس بأذكى وأقوى طريقة عملية.
- عدم حذف ملفات PDF الأصلية.
- تحمل المسؤولية الكاملة عن التخطيط والتنفيذ والاختبار.

ثم أكد المستخدم لاحقا:

- لا يريد تعقيدا في تغيير المفتاح.
- التطبيق خاص.
- مفتاح API يجب أن يحفظ على جهاز الشخص الذي يستخدم البرنامج نفسه.
- يجب استعمال الوكلاء عند الحاجة، لكن لا تطول المحاكاة.
- يجب رفع التحديث وإعطاؤه الرابط ليجرب مباشرة.

## حالة التطبيق القديم قبل الإعادة

تم فحص المشروع قبل التعديل، وكانت البنية القديمة كالتالي:

- `server.mjs` خادم HTTP يدوي.
- واجهة Vanilla JavaScript قديمة في `public/app.js` و`public/index.html` و`public/style.css`.
- فهرس تشغيل قديم اسمه `fast-index.json` يعتمد على المرور على الصفحات.
- فهارس مشتقة متعددة وغير موحدة داخل `curriculum-library/index`.
- إعدادات المزود كانت قابلة للتعارض من المتصفح.
- كان هناك احتمال تسريب المفتاح إلى Endpoint يحدده المستخدم.
- محلل SSE القديم كان حساسا للفواصل ونمط CRLF.
- Service Worker كان يستخدم Cache ثابتا قد يعرض JavaScript قديما.
- `/api/health` كان يفحص الخادم فقط ولا يفحص سلامة الفهرس.
- كانت إعادة المحاولة ممكن أن تتكرر بعد بدء الرد.

## بيانات المنهج المحفوظة

- ملفات PDF الأصلية محفوظة كما هي داخل `curriculum-library/pdf-sources`.
- عدد ملفات PDF: 12.
- إجمالي الصفحات المفهرسة: 1456.
- الصفحات القابلة للبحث من المصدر: 1241 تقريبا، مع بقاء الصفحات المصورة وعدم إسقاطها.
- ملفات `pdf-books` تحتوي بيانات الصفحات والنصوص المستخرجة.
- لم يتم حذف أي PDF.
- الملفات القديمة مثل `fast-index.json` وبعض الفهارس والمراجعات القديمة ما زالت موجودة، لكن التطبيق الجديد لا يعتمد عليها.
- الفهرس canonical المستخدم حاليا هو `curriculum-library/search-index.json`.

## الخطة التي نُفذت

1. فحص المستودع والكتب والفهارس وأوامر التشغيل.
2. تصميم محرك معرفة منفصل عن الخادم والواجهة.
3. توليد فهرس موحد سريع من `pdf-index.json`.
4. إعادة بناء API ومسار المحادثة والبث.
5. إعادة بناء الواجهة بالكامل للجوال أولا.
6. إضافة الاختبارات والتحقق من البناء.
7. تشغيل التطبيق محليا وفحصه عبر متصفح حقيقي.
8. رفع commit إلى GitHub ليعيد Render البناء تلقائيا.

## الفهرس الجديد

الملف: `curriculum-library/search-index.json`

خصائصه:

- `schemaVersion: 3`.
- 12 كتابا و1456 وثيقة صفحة.
- 26180 مصطلحا.
- قاموس `postings` مقلوب يربط الكلمة بالصفحات التي تحتويها.
- `documentFrequency` لحساب ندرة الكلمات.
- `documents` لكل صفحة وتشمل الكتاب والصفحة الفيزيائية والمطبوعة والعنوان والقسم والوحدة والملخص والمعاينة والتوكنز وحالة OCR.
- `printed` لخريطة الصفحة المطبوعة إلى الصفحة الفيزيائية.
- البحث يستخدم ترتيب BM25 مبسطا مع مكافآت للعنوان والعبارة واسم المادة ورقم الصفحة والفرع.
- يدعم أسماء المواد والاختصارات الشائعة مثل العربي والإنكليزي والإسلاميات والجغرافيا.
- يرفع الصفحة المطابقة عندما يكتب المستخدم مثلا: `اشرح صفحة 8 من أسلوب الاستفهام`.
- لا يقرأ ملفات الكتاب الكبيرة إلا عند طلب الصفحة المطلوبة، مع Cache لآخر كتب مستخدمة.
- الصفحة المصورة لا تفشل بصمت، بل تعاد مع ملخص وحالة `needsVision`.

مولد الفهرس:

- `scripts/build-index.mjs`.
- الأمر: `npm run build-index`.
- الأمر الكامل: `npm run verify`.

## الخادم والـ API

الملف الرئيسي: `server.mjs`.

المسارات الحالية:

- `GET /api/health`: حالة الخادم والفهرس وتهيئة الذكاء.
- `GET /api/bootstrap`: إعداد الواجهة وقائمة الكتب والمواد والإحصاءات.
- `GET /api/config`: نفس بيانات التهيئة الأساسية للتوافق.
- `GET /api/books`: قائمة الكتب مع فلترة الفرع والمادة.
- `GET /api/search?q=...`: بحث محلي سريع.
- `GET /api/page?bookId=...&page=...`: فتح صفحة فيزيائية.
- `GET /api/page?bookId=...&printed=...`: فتح صفحة مطبوعة مباشرة.
- `POST /api/chat`: محادثة SSE مع مصادر قبل الرد.

إجراءات الاستقرار والأمان:

- حدود لحجم Body والصور والرسائل.
- حد استخدام بسيط للمحادثات.
- Headers أمنية مثل `X-Content-Type-Options` و`X-Frame-Options`.
- لا يسمح للمستخدم بتغيير Endpoint أو Model من المتصفح.
- مفتاح الجهاز فقط هو الاستثناء المقصود للتطبيق الخاص.
- جلسة محادثة عبر `X-Session`.
- Heartbeat للبث.
- إلغاء الطلب عند إغلاق العميل.
- مهلة حقيقية للمزود تشمل قراءة البث.
- إعادة المحاولة فقط قبل وصول نص، حتى لا تتكرر إجابة بدأت بالفعل.
- محلل SSE يدعم LF وCRLF وJSON fallback.
- يوجد fallback محلي من ملخصات الكتب إذا تعذر المزود أو كان المفتاح غير صالح، بدلا من انهيار التطبيق.

## إعداد الذكاء الاصطناعي

الملف: `lib/config.mjs`.

الإعدادات البيئية:

- `AI_API_KEY`: مفتاح الخادم الاختياري.
- `AI_ENDPOINT`: الافتراضي `https://opencode.ai/zen/go/v1`.
- `AI_MODEL`: الافتراضي `mimo-v2.6-flash`.
- الاسم القديم `MiMo-V2.6-Flash` يحول تلقائيا إلى الاسم الصغير الصحيح.
- `AI_MAX_OUTPUT_TOKENS`: الحد الافتراضي 3000.
- `AI_TIMEOUT_MS`: مهلة المزود الافتراضية 75000 ملي ثانية.
- `RATE_LIMIT_PER_MINUTE`: الافتراضي 18.

### مفتاح المستخدم المحلي

بما أن التطبيق خاص، يوجد حقل واضح داخل الإعدادات:

- العنصر: `#providerKey`.
- التخزين: `localStorage` تحت المفتاح `mualimi.v3`.
- الاسم داخل البيانات: `settings.apiKey`.
- الحفظ يتم من زر `حفظ المفتاح`.
- حذف المفتاح يتم بمسح الحقل ثم الضغط على الحفظ.
- المفتاح لا يكتب في الملفات ولا يدخل Git.
- الواجهة ترسل المفتاح إلى نفس الخادم في Header اسمه `X-AI-API-Key` عند طلب البحث أو الصفحة أو المحادثة.
- الخادم يستخدم مفتاح الجهاز أولا ثم `AI_API_KEY` كاحتياط.
- لا يوجد `CURRICULUM_ADMIN_TOKEN` ولا نظام مسؤول إضافي؛ تمت إزالته لأن المستخدم طلب البساطة.

> ملاحظة أمنية: تم إرسال مفتاح API أثناء المحادثة، لذلك لا تحفظ قيمته هنا ولا في أي commit. يجب إلغاؤه من المزود وإنشاء مفتاح جديد.

## الواجهة الجديدة




- `public/index.html`.
- `public/app.js`.
- `public/style.css`.
- `public/sw.js`.
- `public/manifest.webmanifest`.


- اختيار الفرع: أحيائي أو تطبيقي أو أدبي.
- صفحة بداية بجلسة اليوم وإجراءات سريعة.
- محادثة عربية مع بث حي.
- مصادر قابلة للفتح أسفل الرد.
- فتح النص الكامل للصفحة في نافذة مريحة.
- مكتبة الكتب والبحث والفلترة حسب المادة.
- خطة امتحانات محلية مع عداد الأيام.
- سجل المحادثات والبحث فيه.
- حفظ المحادثات والخطة على الجهاز.
- إرفاق صورة واحدة بعد ضغطها.
- Quiz تفاعلي إذا أعاد النموذج كتلة `quiz` صحيحة.
- Markdown آمن مع منع HTML غير الموثوق.
- حالات اتصال وتحميل وفشل واضحة.
- واجهة Desktop مع Sidebar.
- واجهة Mobile مع Bottom Navigation وComposer مناسب للجوال.
- PWA وService Worker مع Cache جديد باسم `mualimi-v3-1`.

## التخزين المحلي


  - `m2_settings`.
  - `m2_convs`.
  - `m2_exams`.
- الحد المحلي: 30 محادثة، وآخر 80 رسالة لكل محادثة.
- الصور لا تحفظ داخل المحادثات بعد الإرسال.

## الاختبارات والتحقق

آخر نتيجة مؤكدة:



- `npm run verify`: ناجح.
- الاختبارات: 14 ناجحة، 0 فاشلة.
- فحص syntax لـ `server.mjs` و`lib/config.mjs` و`public/app.js`: ناجح.
- اختبار التطبيع العربي وOCR وأرقام الصفحات: ناجح.
- اختبار الفهرس canonical: ناجح.
- اختبار البحث السريع: ناجح.
- اختبار العثور على صفحة مطبوعة: ناجح.
- اختبار الصفحة المصورة: ناجح.
- اختبار خريطة الصفحة والمصادر: ناجح.
- اختبار SSE وCRLF/JSON fallback وإعادة المحاولة: ناجح.

تم فحص التطبيق فعليا عبر متصفح حقيقي:


- شاشة البداية.
- اختيار الفرع.
- الصفحة الرئيسية.
- المكتبة.
- البحث عن `أسلوب الاستفهام`.
- فتح صفحة الكتاب في Modal.
- فتح جلسة جديدة.
- ظهور حالة المصادر في المحادثة.
- اكتشاف أن مفتاح الاختبار القديم كان يرفضه المزود، وإضافة fallback محلي بدلا من فشل التطبيق.

## النشر

تم تنفيذ:

```bash
git add -A
git -c user.name='Mualimi' -c user.email='deploy@mualimi.local' commit -m "إعادة بناء معلمي وتبسيط إعداد المفتاح"
git push
```

الفرع المرفوع: `main`.

Render يعيد البناء تلقائيا بعد push. تم التحقق من النسخة المنشورة بعد الرفع، وكان `/api/health` يعيد:

- `schemaVersion: 3`.
- 12 كتابا.
- 1456 صفحة.
- 26180 مصطلحا.

## أوامر العمل القادمة

```bash
npm start
npm run build-index
npm test
npm run verify
```

للنشر بعد أي تعديل:

```bash
git add -A
git -c user.name='Mualimi' -c user.email='deploy@mualimi.local' commit -m "وصف التعديل"
git push
```

## قواعد مهمة لأي مطور لاحق

- لا تحذف ملفات `curriculum-library/pdf-sources`.
- لا تضع أي API key داخل Git أو ملفات الواجهة الثابتة.
- لا تعيد نظام Admin أو Token لتغيير المفتاح؛ المطلوب الحالي حقل محلي بسيط.
- لا تسمح بتغيير Endpoint من المتصفح.
- شغل `npm run verify` قبل الرفع.
- لا تستبدل الفهرس canonical بفهرس مسح شامل للصفحات في كل طلب.
- عند تغيير شكل الفهرس شغل `npm run build-index` ثم اختبر `search` و`fullPage` و`retrieveContext`.
- عند تغيير الواجهة حافظ على RTL والجوال أولا.
- لا تسجل نصوص المفاتيح أو Headers الحساسة في Logs.
